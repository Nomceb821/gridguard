// Point this at your deployed backend (e.g. Render URL) once it's live.
// For local development, run the backend with `uvicorn app.main:app --reload`
// and leave this as localhost.
const API_BASE_URL = 'http://localhost:8000';
const WS_BASE_URL = 'ws://localhost:8000';